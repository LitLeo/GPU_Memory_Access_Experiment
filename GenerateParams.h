#include <iostream>
#include "Case.h"
#include <fstream>
using namespace std;
#ifndef LOOP
#define LOOP 50  // 因为使用了warmup函数，所以就不需要循环多次了
#endif

#ifndef GENERATE_PARAMS
#define GENERATE_PARAMS   // 因为使用了warmup函数，所以就不需要循环多次了

void generate_params();
 

#endif

